# Anthropic API key — used by the summarizer to call the Messages API.
ANTHROPIC_API_KEY=

# Base URL of your external case management API (no trailing slash).
# e.g. https://api.example-ccaas.com
CASE_API_BASE_URL=

# Bearer token used to authenticate against the case API.
CASE_API_TOKEN=
